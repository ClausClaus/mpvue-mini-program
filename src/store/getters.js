export const count = state => state.count;

export const article = state => state.article;

export const isPlayingMusic = state => state.isPlayingMusic;

export const currentMusicId = state => state.currentMusicId;
